# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Temporary files
*.tmp
*.temp
*~
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local test files
/tmp/

# Generated site output (built fresh by build.sh in CI on every deploy —
# see .github/workflows/deploy.yml. Not needed in version control, and
# committing it just duplicates build.sh's own output as a second source
# of truth that can drift.)
/docs/
